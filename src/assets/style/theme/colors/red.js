import { red as muiRed } from "@mui/material/colors"
import c from "../../../../constants"

const red = {
  label: c.redLabel,
  light: muiRed[200],
  main: muiRed[500],
  dark: muiRed[800],
}

export default red
