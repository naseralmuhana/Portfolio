import { purple as muiPurple } from "@mui/material/colors"
import c from "../../../../constants"

const purple = {
  label: c.purpleLabel,
  light: muiPurple[200],
  main: muiPurple[500],
  dark: muiPurple[800],
}

export default purple
