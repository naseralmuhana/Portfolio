import { orange as muiOrange } from "@mui/material/colors"
import c from "../../../../constants"

const orange = {
  label: c.orangeLabel,
  light: muiOrange[200],
  main: muiOrange[500],
  dark: muiOrange[800],
}

export default orange
