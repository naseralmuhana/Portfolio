import { blue as muiBlue } from "@mui/material/colors"
import c from "../../../../constants"

const blue = {
  label: c.blueLabel,
  light: muiBlue[200],
  main: muiBlue[500],
  dark: muiBlue[800],
}

export default blue
