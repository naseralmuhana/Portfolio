import { teal as muiTeal } from "@mui/material/colors"
import c from "../../../../constants"
const teal = {
  label: c.tealLabel,
  light: muiTeal[200],
  main: muiTeal[500],
  dark: muiTeal[800],
}

export default teal
